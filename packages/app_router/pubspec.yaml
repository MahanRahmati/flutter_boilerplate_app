name: app_router
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  # Routing
  go_router: ^14.6.2

  screen_splash:
    path: ../../packages/screen_splash

  screen_home:
    path: ../../packages/screen_home

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  build_runner: ^2.4.13
  go_router_builder: ^2.3.0
