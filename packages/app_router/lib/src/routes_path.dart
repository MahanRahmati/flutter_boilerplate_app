/// RoutesPath class defines the path constants used for routing.
///
/// This class contains String constants for the different route paths used
/// in the application.
///
/// These path constants are referenced by the router to build the routes.
/// Using constants avoids typos and ensures route paths are consistent.
class RoutesPath {
  RoutesPath._();

  static const String splash = '/';
  static const String home = '/home';
}
