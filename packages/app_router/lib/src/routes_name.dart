/// RoutesName class defines the route name constants used for routing.
///
/// This class contains String constants for the different route names used
/// in the application.
///
/// These name constants are referenced by the router to identify routes.
/// Using constants avoids typos and ensures route names are consistent.
class RoutesName {
  RoutesName._();

  static const String splash = 'splash';
  static const String home = 'home';
}
